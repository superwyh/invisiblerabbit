import React from 'react';
import { TimelineData } from '../types';

// Kept for future interactive demo sections if needed
const TimelineView: React.FC<{ timeline: TimelineData; isActive: boolean }> = () => {
  return null;
};

export default TimelineView;